@model IEnumerable<MVCLab04.Models.CourseRegistration>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Grade)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Grade)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CourseRegistrationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CourseRegistrationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CourseRegistrationID })
        </td>
    </tr>
}

</table>
